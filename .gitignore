# Jupyter Notebook
.ipynb_checkpoints
.virtual_documents
doc_classification.egg-info/
__pycache__

.vscode
.idea
.DS_Store
.venv


logs/
env_mlops/

/predict
/env_predict
/frontend
/orchestrator

/mlruns

.env
.env_original
.env_mlops_sake
.env_plantUML

dockers_started.txt

docker/eval/private_conn_dagshub.sh

NOT_USED_FOR_DAGSHUB/


# generate a key with ssh
# ssh-keygen -t rsa -b 4096 -C "yourEmail" --> save it with no passphrase
# put the private key 'id_rsa' in the docker/admin-backend/ folder 
# and the public key 'id_rsa_pub' in your github account -> https://github.com/settings/keys
docker/admin-backend/id_rsa

# DO NOT MODIFY : these are created inside the admin backend docker and should not be tracked by git
private_conn_dagshub.sh

csv_files
secrets
img
corrections